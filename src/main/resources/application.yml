server:
  port: 8011
spring:
  datasource: 
     url: jdbc:mysql://localhost:3306/db_excel?rewriteBatchedStatements=true
     username: root
     password: 184703
     hikari:
       maximum-pool-size: 20
       minimum-idle: 20
  jpa:
    hibernate:
        ddl-auto: none
        jdbc:
          "[batch_size]": 20
          "[order_inserts]": true
          "[order_updates]": true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
