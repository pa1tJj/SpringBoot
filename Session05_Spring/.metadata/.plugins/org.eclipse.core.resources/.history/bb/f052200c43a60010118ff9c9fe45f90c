package com.jone.spring.spring_to_go.noioc;

public class ConstractService {

	//code chính của mình, và mình lo CRUD table Contract qua Repo
	//sau đó mình sẽ generate pdf. Nhờ PdfGenerate giúp, vậy PdfGenerate là dependency và tự new ở đây hoặc đc tiêm/chích vào
	
	private PdfGenerator pdfGenerator;
	
	public ConstractService (PdfGenerator pdfGenerator) {
		this.pdfGenerator = pdfGenerator;
	}
	
	public void processConstract() {
		//TODO: Logic xử lí
		
		pdfGenerator.generateFile("24935395834954354");
	}
}
